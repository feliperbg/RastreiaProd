RewriteEngine on

# Se o arquivo ou diretório existir, não redireciona
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redireciona todas as outras requisições para app.js
RewriteRule ^(.*)$ /app.js [QSA,L]